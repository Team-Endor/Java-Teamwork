package models.enemies;

import java.awt.image.BufferedImage;

public class FighterPlane extends Enemy {
    public FighterPlane(int x, int y, BufferedImage image, int velocity) {
        super(x, y, image, velocity);
    }
}

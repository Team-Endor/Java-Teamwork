package models.enemies;

import java.awt.image.BufferedImage;

public class GroundRocket extends Enemy {
    public GroundRocket(int x, int y, BufferedImage image, int velocity) {
        super(x, y, image, velocity);

    }
}
